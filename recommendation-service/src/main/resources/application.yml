spring:
  application:
    name: recommendation-service
  datasource:
    url: jdbc:mysql://mysql:3306/recommendation_db
    username: root
    password:
  flyway:
    enabled: true
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  server:
    port: 8091

  eureka:
    client:
      service-url:
        defaultZone: http://mysql:8761/eureka

  springdoc:
    api-docs:
      enabled: true
    swagger-ui:
      enabled: true
      path: /swagger-ui.html

  management:
    endpoints:
      web:
        exposure:
          include: health
    endpoint:
      health:
        show-details: always

  logging:
    file:
      name: logs/recommendation-service.log
    level:
      root: INFO
      com.library.recommendationservice: DEBUG